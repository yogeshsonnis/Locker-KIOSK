<UserControl x:Class="Locker_KIOSK.Views.ScanBarcodeViews.OOHPODScan"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Locker_KIOSK.Views.ScanBarcodeViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#A3D8FF" Offset="0"/>

                <GradientStop Color="#F7FCFF" Offset="1"/>

            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Please Scan Your barcode below" FontSize="32" FontWeight="Bold" Foreground="#564B51" HorizontalAlignment="Center"  Margin="0,41,0,0"/>
        <Image Source="/Images/ScanBarcode.png" Grid.Row="1" Stretch="UniformToFill" Height="259" Width="415" HorizontalAlignment="Center" Margin="0,62,0,0"/>
        <StackPanel Grid.Row="2"  HorizontalAlignment="Center" Orientation="Vertical" Margin="0,40,0,0">
            <TextBlock Text="or" FontSize="32" Foreground="#564B51" FontWeight="Bold" HorizontalAlignment="Center"  Margin="0,0,0,26"/>
            <Button Grid.Row="1" Style="{StaticResource ManuallButton}" Command="{Binding EnterManualNextCommand}">
                <StackPanel Orientation="Horizontal" >
                    <TextBlock Text="Enter manually"  Margin="0,0,10,0" />

                    <Path Data="M2 14H34M34 14L22 2M34 14L22 26" Stroke="#564B51" StrokeThickness="3.5" Width="32" Height="24" Stretch="UniformToFill" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>

    </Grid>
</UserControl>
